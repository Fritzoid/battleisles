[package]
name = "battleisles_game"
version = "0.1.0"
edition = "2021"

[dependencies]
battleisles_domain = { path = "../battleisles_domain", version = "0.1.0" }
getrandom = { version = "0.3", features = ["wasm_js"] }
bevy = "=0.15.2"
bevy_egui = "0.33.0"
